import tkinter as tk

root = tk.Tk()
root.title('Yappers')
root.geometry('900x500') # good window size imo
root.iconbitmap('./assets/pythontutorial.ico') # app icon -- later priority
root.mainloop() # compiles everything and opens the window of the app!!!


